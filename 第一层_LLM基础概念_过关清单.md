# 第一层 · LLM 基础概念过关清单

> 用途：第 1-4 周，和 Python 学习并行。吃透这份文档 = 第一层毕业。
> 合格线（费曼标准）：**不翻笔记，能用你自己的话向一个完全不懂的人讲清楚。**
> 不合格：只记住名词定义、说不出所以然。
> 节奏：每周 2 个概念，每个花 30-60 分钟。先读 → 合上文档用自己的话讲一遍 → 答自检题。

---

## 总纲：第一层的合格标准

学完这层，你要能回答四个问题：
1. LLM 到底是个什么东西？擅长什么、不擅长什么？
2. 为什么它答错还理直气壮（幻觉）？有什么办法让它更可靠？
3. "指挥它"的旋钮有哪些（prompt、temperature、上下文）？各自管什么？
4. 为什么 agent 开发绕不开这些概念？

---

## 概念清单

### 第 1 周

**概念 1：LLM 是什么**

- **本质**：一台"预测下一个词"的巨大机器。你给它一段文字，它算出最可能的下一个词，再接下一个，不断接，直到停。
- **为什么重要**：理解了"它只是在预测下一个词"，你才能解释它所有的怪行为——答非所问、幻觉、编造引用。
- **过关标准**：
  - 能用一句话解释"为什么它会一本正经地胡说八道"（因为它不是"知道答案"，是在"预测合理的下一段话"）
  - 能说出"训练"和"推理"的区别（训练=喂海量文字学规律；推理=用训练好的模型生成）
  - 知道"模型不是数据库"：知识有截止日期，不能当事实库查
- **自检**：有人说"ChatGPT 什么都知道"，你怎么纠正他？

**概念 2：Token**

- **本质**：模型处理文本的最小单位，不是"字"也不是"单词"。中文约 1 个字 ≈ 1 个 token，英文约 1 个词 ≈ 1.3 个 token。
- **为什么重要**：token = 成本。每次调 API 都按 token 收费，输入输出都算。提示词越长越贵。
- **过关标准**：
  - 能估算"一句 100 字的中文"大概多少 token（约 100-150）
  - 知道"把一大段文档塞进提示词"意味着什么（贵、占窗口）
  - 知道 token 和"字数"是两回事
- **自检**：为什么翻译软件按"字数"收费，而 LLM 按 token 收费？

### 第 2 周

**概念 3：上下文窗口（Context Window）**

- **本质**：模型一次能"看到"的全部文字，输入+输出合计有上限（如 128k token）。超了就得扔一部分。
- **为什么重要**：窗口就是模型"当下的记忆"。塞满后它会忘掉最早的内容。
- **过关标准**：
  - 能说出"上下文满了"的 3 种处理办法：截断最早的 / 把历史总结成摘要 / 用检索（引出 RAG）
  - 知道为什么长对话会"越聊越笨"（窗口被历史占满，留给新信息的空间变小）
  - 知道为什么 agent 必须管理上下文（不能无限堆）
- **自检**：一个客服 agent 聊了 100 轮，为什么开始答非所问？怎么修？

**概念 4：Prompt 结构（提示词）**

- **本质**：给模型的"说明书"。分三部分：系统提示词（system，设定角色和全局规则）、用户输入（user）、模型回复（assistant）。
- **为什么重要**：agent 开发里 90% 的"效果不好"是提示词没写好，不是模型不行。
- **过关标准**：
  - 能写一个规范三段式：**角色 + 任务 + 输出格式**
  - 知道给例子（few-shot）比只描述要求更有效
  - 知道"限定范围"（不知道就说不知道）能压幻觉
- **自检**：写一个"医院分诊助手"的系统提示词，要包含角色、任务、输出格式。

### 第 3 周

**概念 5：采样参数 temperature**

- **本质**：控制"随机程度"的旋钮。越低越稳定保守、越接近"最可能的答案"；越高越天马行空。
- **为什么重要**：工具调用、事实问答要低（否则参数会传错）；创意文案可适度高。
- **过关标准**：
  - 给场景能选 temperature：让 agent 查数据库 → 低；让模型写营销标题 → 可以稍高
  - 知道 top_p 是另一个类似旋钮，和 temperature 不要同时调
- **自检**：你的 agent 负责查库存，应该用高还是低 temperature？为什么？

**概念 6：幻觉（Hallucination）与事实性**

- **本质**：模型"自信地编造"。因为它不是在查答案，而是在生成"像答案的东西"。
- **为什么重要**：agent 一旦把编的东西当真的执行，会闯祸（报错库存、给错诊断）。幻觉是 agent 工程的头号问题。
- **过关标准**：
  - 能说出至少 3 种缓解手段：① 提示词让它"不知道就说不知道" ② 要求它给出来源依据 ③ 用工具调用/RAG 去取真实数据，而不是让它凭记忆答
  - 知道"纯靠模型记忆的事实，都不可完全信任"
- **自检**：一个医疗问答 agent 编了个药品副作用，你会怎么防？

### 第 4 周

**概念 7：结构化输出（JSON / 结构化数据）**

- **本质**：让模型输出"程序能解析"的格式（JSON），而不是一段自由文字。配合 function calling 使用。
- **为什么重要**：agent 里模型要和代码打交道——代码要读它的输出做下一步，自由文本没法解析。
- **过关标准**：
  - 知道为什么让模型输出 JSON 比输出散文可靠（格式约束 + 程序可解析）
  - 知道 "function calling" 是让模型决定"调用哪个函数、填什么参数"，返回值再喂回给它
  - 能说出 agent 循环的一小步：模型选工具 → 代码执行 → 结果返回给模型
- **自检**：为什么不能让模型"自由地"决定调用哪个工具？

**概念 8：成本与 token 经济学**

- **本质**：输入 token 比输出 token 便宜；提示词越长越贵；调用越频繁越贵。
- **为什么重要**：agent 会自动调很多次模型（一个循环几十次调用），成本比聊天高得多，必须心里有数。
- **过关标准**：
  - 知道"每轮都塞全部历史对话"为什么贵，怎么省（截断、摘要）
  - 知道 agent 的"思考过程"也是 token，会花钱
  - 有"省 token"意识：提示词精简、缓存复用
- **自检**：一个 agent 在 100 步循环里每一步都带完整历史，你怎么优化成本？

---

## 预告概念（知道有这回事即可，第 5 层才深入）

- **流式输出（streaming）**：为什么聊天界面是一个字一个字蹦出来的——模型边生成边传，提升体验。
- **Embedding**：把文本变成向量，语义相近的向量距离近——这是后面 RAG（让 agent"读过文档"）的基础。

---

## 期末过关自测（第 4 周做完，第一层才算毕业）

1. 用你自己的话，向一个完全不懂的人解释：LLM 是什么、为什么它会幻觉、怎么让它更可靠。
2. 写一个"医院分诊助手"系统提示词（角色 + 任务 + 输出格式），交给 Claude 评判。
3. 三个场景选 temperature：
   a. agent 查询数据库 → ＿
   b. 生成广告文案 → ＿
   c. 解析用户输入做意图判断 → ＿
4. 一个 agent 的聊天窗口已满，你有哪几种处理办法？
5. 为什么开发 agent 要先学这些概念，而不是直接学 LangChain？（用自己的话）

---

## 推荐阅读（概念看不明白时再翻）

- 吴恩达《ChatGPT Prompt Engineering for Developers》第 1-2 课
- OpenAI 官方文档：Prompt engineering 指南
- Anthropic 官方文档：Introduction to LLMs（概念部分）

---

## 过关勾选表

- [ ] 第 1 周：概念 1 □  概念 2 □
- [ ] 第 2 周：概念 3 □  概念 4 □
- [ ] 第 3 周：概念 5 □  概念 6 □
- [ ] 第 4 周：概念 7 □  概念 8 □
- [ ] 期末自测：全对 □

全部勾上 = 第一层毕业，进入第二层（调 LLM API）。
